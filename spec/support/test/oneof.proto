package test;

message FullName {
  optional string first_name = 1;
  optional string last_name = 2;
}

message OneofTest {
  oneof SingleIdentifier {
    FullName full_name = 1;
    string email = 2;
    string username = 3;
  }
}
